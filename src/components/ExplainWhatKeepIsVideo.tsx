export function ExplainWhatKeepIsVideo() {
  return (
    <video
      className="z-100 bg-video-background relative top-0 max-h-[calc(100vh-120px)] w-full object-contain"
      autoPlay
      loop
      muted
      playsInline
      src="/videos/how-it-works-keep-animation.mp4"
    >
      Your browser does not support the video tag
    </video>
  )
}
